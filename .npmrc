# Generated lockfiles and installs are reproducible or they are not builds.
save-exact=true
# A postinstall script is arbitrary code execution at install time on every
# developer machine and every CI runner. Nothing in this workspace needs one.
ignore-scripts=true
